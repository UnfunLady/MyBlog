---
title: "一些常用的操作"
catalog: true
date: 2024-06-07 19:29:03
subtitle: "常用方法"
header-img:
tags:
  - uni-app
  - JavaScript
catagories:
  - JavaScript
---

### 笔记描述

> 一些项目常有的 js 方法

### 主要代码

```js
// 金额相关 数字加逗号  123456=>123,456
const formatPrice = (key) => {
  key += "";
  let x = key.split(".");
  let x1 = x[0];
  let x2 = x.length > 1 ? "." + x[1] : "";
  let reg = /(\d+)(\d{3})/;
  while (reg.test(x1)) {
    x1 = x1.replace(reg, "$1" + "," + "$2");
  }
  return x1 + x2;
};

// 金额相关 数字加逗号  123456=>123,456
const formatPrice = (key) => {
  key += "";
  let x = key.split(".");
  let x1 = x[0];
  let x2 = x.length > 1 ? "." + x[1] : "";
  let reg = /(\d+)(\d{3})/;
  while (reg.test(x1)) {
    x1 = x1.replace(reg, "$1" + "," + "$2");
  }
  return x1 + x2;
};
// 判断是否是JSON
function isJSON(str) {
  if (typeof str == "string") {
    try {
      JSON.parse(str);
      return true;
    } catch (e) {
      console.log(e);
      return false;
    }
  }
  console.log("It is not a string!");
}
// uni复制文本
const copyText = (text) => {
  uni.setClipboardData({
    data: text,
    success: function () {
      uni.showToast({
        title: "复制成功",
        icon: "none",
        duration: 2000,
      });
    },
    fail: (e) => {
      console.log(e);
      uni.showToast({
        title: "复制失败",
        icon: "none",
        duration: 2000,
      });
    },
  });
};

// 获取大额数字单位
const getCountUnit = (number) => {
  if (Number(number) >= 0 && Number(number) <= 9) {
    return "";
  }
  try {
    const str = String(number).split(".");
    const len = str[0].length;
    switch (len) {
      case 1:
        return "";
        break;
      case 2:
        return "";
        break;
      case 3:
        return "";
        break;
      case 4:
        return "";
        break;
      case 5:
        return "万";
        break;
      case 6:
        return "十万";
        break;
      case 7:
        return "百万";
        break;
      case 8:
        return "千万";
        break;
      case 9:
        return "亿";
        break;
      case 10:
        return "十亿";
        break;
      case 11:
        return "百亿";
        break;
      default:
        return "未知单位";
        break;
    }
  } catch (e) {
    //TODO handle the exception
    return "未知单位";
  }
```
