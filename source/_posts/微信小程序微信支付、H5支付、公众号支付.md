---
title: "微信支付、H5支付、公众号支付"
catalog: true
date: 2024-07-16 20:11:03
subtitle: "微信支付方式"
header-img:
tags:
  - uni-app
  - JavaScript
catagories:
  - JavaScript
---

### 笔记描述

> 用于支付相关

### 主要代码

```js
// 引入sdk
// http://res.wx.qq.com/open/js/jweixin-1.6.0.js

// 引入依赖
import jsrsasign from "jsrsasign";

// 随机字符串
function getRandomString(length) {
  var result = "";
  var characters =
    "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
  var charactersLength = characters.length;
  for (var i = 0; i < length; i++) {
    result += characters.charAt(Math.floor(Math.random() * charactersLength));
  }
  return result;
}

// 签名
function signH5(data) {
  let encipher = new jsrsasign.RSAKey();
  // 私钥 在微信端申请好后会有，一般由后端提供
  const key = `-----BEGIN PRIVATE KEY----------END PRIVATE KEY-----`;
  // 把私钥转成16进制并设置秘钥
  encipher = jsrsasign.KEYUTIL.getKey(key);
  // 设置SHA256withRSA方式加密
  const sig = new jsrsasign.KJUR.crypto.Signature({
    alg: "SHA256withRSA",
  });
  // 初始化
  sig.init(encipher);
  // 需要加密的字段，这里注意下一定要按每一个字段一行，切末位加上 \n
  const str =
    `${data.appId}\n` +
    `${data.timeStamp}\n` +
    `${data.nonceStr}\n` +
    `${data.package}\n`;
  const paySign = sig.updateString(str);
  // 加密后,转成base64
  return jsrsasign.hextob64(sig.sign());
}

// webview中判断环境
const ua = navigator.userAgent.toLowerCase();
if (ua.match(/MicroMessenger/i) == "micromessenger") {
  //ios的ua中无miniProgram，但都有MicroMessenger（表示是微信浏览器）
  wx.miniProgram.getEnv((res) => {
    if (res.miniprogram) {
      //在微信内，在小程序内。
      let str = {
        appId: "小程序APPid",
        timeStamp: Math.round(new Date().getTime() / 1000).toString(),
        nonceStr: getRandomString(10),
        package: "prepay_id=" + "后端返回的凭证",
      };
      const paySign = signH5(str);
      uni.requestPayment({
        ...str,
        signType: "RSA",
        paySign,
        success: (r) => {
          uni.showToast({
            title: "支付成功",
            icon: "none",
          });
        },
        fail: (e) => {
          uni.showToast({
            title:
              e.errMsg && e.errMsg == "requestPayment:fail cancel"
                ? "用户取消支付"
                : "支付失败",
            icon: "none",
          });
        },
      });
    } else {
      // 微信浏览器环境
      //在微信内，不在小程序内。公众号支付
      WeixinJSBridge.invoke(
        "getBrandWCPayRequest",
        {
          ...str,
          signType: "RSA", //微信签名方式：
          paySign,
        },
        function (res) {
          if (res.err_msg == "get_brand_wcpay_request:fail") {
            //  支付失败 请重试
            return;
          }
          if (res.err_msg == "get_brand_wcpay_request:cancel") {
            // 用户取消支付
            return;
          }
          if (res.err_msg == "get_brand_wcpay_request:ok") {
            // 支付成功
          }
        }
      );
    }
  });
} else {
  //不在微信内
  // H5支付
  window.location.href = "后端返回的支付地址";
}
```
